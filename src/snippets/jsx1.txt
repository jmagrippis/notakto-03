// src/App.tsx
import React, { PureComponent } from 'react'

export class App extends PureComponent {
  // they can implement "Lifecycle Methods" you do not need to add,
  // like `componentDidMount`

  // they have to implement a `render` method,
  // which defines what is shown on screen
  render() {
    return <h1>Notakto</h1>
  }
}
